#include <stdio.h>
#include <stdlib.h>

int main()
{
	int i=1,x=0;
	while(i<10){
		int j=1;
		if(!x) x++;
		else printf("\n");
		while(j<10){
			printf("%d *%2d = %2d\t%d *%2d = %2d\t%d *%2d = %2d\n",i,j,i*j,i+1,j,(i+1)*j,i+2,j,(i+2)*j);
			j++;
		}
		i += 3;
	}
}

